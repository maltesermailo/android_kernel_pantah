{
  "comments": [
    {
      "robotId": "ayeaye",
      "robotRunId": "AR00000000000045932051",
      "url": "https://android-build.googleplex.com/builds/treetop/android-review/2527122?revision\u003d3",
      "fixSuggestions": [],
      "key": {
        "uuid": "00455846_28139f9b",
        "filename": "/COMMIT_MSG",
        "patchSetId": 3
      },
      "lineNbr": 0,
      "author": {
        "id": 1757831
      },
      "writtenOn": "2023-06-01T09:28:36Z",
      "side": 1,
      "message": "[Experimental] Cherry pick is not clean. These are the differences between the original change and this cherry-pick:\nChecked against UPSTREAM commit: [googlesource](https://android.googlesource.com/kernel/common/+/ff878b408a03bef5d610b7e2302702e16a53636e) [kernel.org](https://git.kernel.org/pub/scm/linux/kernel/git/torvalds/linux.git/commit/?id\u003dff878b408a03bef5d610b7e2302702e16a53636e)\n\n```\n      ## sound/usb/endpoint.c ##\n     @@ sound/usb/endpoint.c: bool snd_usb_endpoint_compatible(struct snd_usb_audio *chip,\n    @@ sound/usb/endpoint.c: bool snd_usb_endpoint_compatible(struct snd_usb_audio *chi\n       * Note that this function doesn\u0027t configure the endpoint.  The substream\n     - * needs to set it up later via snd_usb_endpoint_configure().\n     + * needs to set it up later via snd_usb_endpoint_set_params() and\n    -+ * snd_usb_endpoint_prepare().\n    ++ * snd_usb_endpoint_configure().\n       */\n      struct snd_usb_endpoint *\n      snd_usb_endpoint_open(struct snd_usb_audio *chip,\n    @@ sound/usb/endpoint.c: out_of_memory:\n      {\n      \tconst struct audioformat *fmt \u003d ep-\u003ecur_audiofmt;\n      \tint err;\n    -@@ sound/usb/endpoint.c: static int init_sample_rate(struct snd_usb_audio *chip,\n    +@@ sound/usb/endpoint.c: static int snd_usb_endpoint_set_params(struct snd_usb_audio *chip,\n      }\n      \n      /*\n     - * snd_usb_endpoint_configure: Configure the endpoint\n    -+ * snd_usb_endpoint_prepare: Prepare the endpoint\n    ++ * snd_usb_endpoint_configure: Prepare the endpoint\n       *\n       * This function sets up the EP to be fully usable state.\n     - * It\u0027s called either from hw_params or prepare callback.\n    @@ sound/usb/endpoint.c: static int init_sample_rate(struct snd_usb_audio *chip,\n       * The function checks need_setup flag, and performs nothing unless needed,\n       * so it\u0027s safe to call this multiple times.\n       *\n    -  * This returns zero if unchanged, 1 if the configuration has changed,\n    +@@ sound/usb/endpoint.c: static int snd_usb_endpoint_set_params(struct snd_usb_audio *chip,\n       * or a negative error code.\n       */\n    --int snd_usb_endpoint_configure(struct snd_usb_audio *chip,\n    + int snd_usb_endpoint_configure(struct snd_usb_audio *chip,\n     -\t\t\t       struct snd_usb_endpoint *ep)\n    -+int snd_usb_endpoint_prepare(struct snd_usb_audio *chip,\n     +\t\t\t     struct snd_usb_endpoint *ep)\n      {\n      \tbool iface_first;\n    @@ sound/usb/endpoint.h: snd_usb_endpoint_open(struct snd_usb_audio *chip,\n      \t\t      bool is_sync_ep);\n      void snd_usb_endpoint_close(struct snd_usb_audio *chip,\n      \t\t\t    struct snd_usb_endpoint *ep);\n    --int snd_usb_endpoint_configure(struct snd_usb_audio *chip,\n    --\t\t\t       struct snd_usb_endpoint *ep);\n     +int snd_usb_endpoint_set_params(struct snd_usb_audio *chip,\n     +\t\t\t\tstruct snd_usb_endpoint *ep);\n    -+int snd_usb_endpoint_prepare(struct snd_usb_audio *chip,\n    + int snd_usb_endpoint_configure(struct snd_usb_audio *chip,\n    +-\t\t\t       struct snd_usb_endpoint *ep);\n     +\t\t\t     struct snd_usb_endpoint *ep);\n      int snd_usb_endpoint_get_clock_rate(struct snd_usb_audio *chip, int clock);\n      \n      bool snd_usb_endpoint_compatible(struct snd_usb_audio *chip,\n     \n      ## sound/usb/pcm.c ##\n    -@@ sound/usb/pcm.c: static int configure_endpoints(struct snd_usb_audio *chip,\n    - \t\tif (stop_endpoints(subs, false))\n    - \t\t\tsync_pending_stops(subs);\n    - \t\tif (subs-\u003esync_endpoint) {\n    --\t\t\terr \u003d snd_usb_endpoint_configure(chip, subs-\u003esync_endpoint);\n    -+\t\t\terr \u003d snd_usb_endpoint_prepare(chip, subs-\u003esync_endpoint);\n    - \t\t\tif (err \u003c 0)\n    - \t\t\t\treturn err;\n    - \t\t}\n    --\t\terr \u003d snd_usb_endpoint_configure(chip, subs-\u003edata_endpoint);\n    -+\t\terr \u003d snd_usb_endpoint_prepare(chip, subs-\u003edata_endpoint);\n    - \t\tif (err \u003c 0)\n    - \t\t\treturn err;\n    - \t\tsnd_usb_set_format_quirk(subs, subs-\u003ecur_audiofmt);\n    - \t} else {\n    - \t\tif (subs-\u003esync_endpoint) {\n    --\t\t\terr \u003d snd_usb_endpoint_configure(chip, subs-\u003esync_endpoint);\n    -+\t\t\terr \u003d snd_usb_endpoint_prepare(chip, subs-\u003esync_endpoint);\n    - \t\t\tif (err \u003c 0)\n    - \t\t\t\treturn err;\n    - \t\t}\n     @@ sound/usb/pcm.c: static int snd_usb_hw_params(struct snd_pcm_substream *substream,\n      \tsubs-\u003ecur_audiofmt \u003d fmt;\n      \tmutex_unlock(\u0026chip-\u003emutex);\n```\n\nPlease refer to the contribution guidelines for details:\nhttps://android.googlesource.com/kernel/common/+/refs/heads/android-mainline/README.md\nAyeAye - CL 536706920 android-build.ayeaye-python-dispatcher_20230531.05_p0",
      "revId": "e111ea4a1f4a4b3333a4c9874fe3056f93f49c05",
      "serverId": "85c56323-6fa9-3386-8a01-6480fb634889"
    },
    {
      "robotId": "ayeaye",
      "robotRunId": "AR00000000000045932051",
      "url": "https://android-build.googleplex.com/builds/treetop/android-review/2527122?revision\u003d3",
      "fixSuggestions": [],
      "key": {
        "uuid": "47344fc1_886d4e93",
        "filename": "/COMMIT_MSG",
        "patchSetId": 3
      },
      "lineNbr": 0,
      "author": {
        "id": 1757831
      },
      "writtenOn": "2023-06-02T08:16:21Z",
      "side": 1,
      "message": "[Experimental] Cherry pick is not clean. These are the differences between the original change and this cherry-pick:\nChecked against UPSTREAM commit: [googlesource](https://android.googlesource.com/kernel/common/+/ff878b408a03bef5d610b7e2302702e16a53636e) [kernel.org](https://git.kernel.org/pub/scm/linux/kernel/git/torvalds/linux.git/commit/?id\u003dff878b408a03bef5d610b7e2302702e16a53636e)\n\n```\n      ## sound/usb/endpoint.c ##\n     @@ sound/usb/endpoint.c: bool snd_usb_endpoint_compatible(struct snd_usb_audio *chip,\n    @@ sound/usb/endpoint.c: bool snd_usb_endpoint_compatible(struct snd_usb_audio *chi\n       * Note that this function doesn\u0027t configure the endpoint.  The substream\n     - * needs to set it up later via snd_usb_endpoint_configure().\n     + * needs to set it up later via snd_usb_endpoint_set_params() and\n    -+ * snd_usb_endpoint_prepare().\n    ++ * snd_usb_endpoint_configure().\n       */\n      struct snd_usb_endpoint *\n      snd_usb_endpoint_open(struct snd_usb_audio *chip,\n    @@ sound/usb/endpoint.c: out_of_memory:\n      {\n      \tconst struct audioformat *fmt \u003d ep-\u003ecur_audiofmt;\n      \tint err;\n    -@@ sound/usb/endpoint.c: static int init_sample_rate(struct snd_usb_audio *chip,\n    +@@ sound/usb/endpoint.c: static int snd_usb_endpoint_set_params(struct snd_usb_audio *chip,\n      }\n      \n      /*\n     - * snd_usb_endpoint_configure: Configure the endpoint\n    -+ * snd_usb_endpoint_prepare: Prepare the endpoint\n    ++ * snd_usb_endpoint_configure: Prepare the endpoint\n       *\n       * This function sets up the EP to be fully usable state.\n     - * It\u0027s called either from hw_params or prepare callback.\n    @@ sound/usb/endpoint.c: static int init_sample_rate(struct snd_usb_audio *chip,\n       * The function checks need_setup flag, and performs nothing unless needed,\n       * so it\u0027s safe to call this multiple times.\n       *\n    -  * This returns zero if unchanged, 1 if the configuration has changed,\n    +@@ sound/usb/endpoint.c: static int snd_usb_endpoint_set_params(struct snd_usb_audio *chip,\n       * or a negative error code.\n       */\n    --int snd_usb_endpoint_configure(struct snd_usb_audio *chip,\n    + int snd_usb_endpoint_configure(struct snd_usb_audio *chip,\n     -\t\t\t       struct snd_usb_endpoint *ep)\n    -+int snd_usb_endpoint_prepare(struct snd_usb_audio *chip,\n     +\t\t\t     struct snd_usb_endpoint *ep)\n      {\n      \tbool iface_first;\n    @@ sound/usb/endpoint.h: snd_usb_endpoint_open(struct snd_usb_audio *chip,\n      \t\t      bool is_sync_ep);\n      void snd_usb_endpoint_close(struct snd_usb_audio *chip,\n      \t\t\t    struct snd_usb_endpoint *ep);\n    --int snd_usb_endpoint_configure(struct snd_usb_audio *chip,\n    --\t\t\t       struct snd_usb_endpoint *ep);\n     +int snd_usb_endpoint_set_params(struct snd_usb_audio *chip,\n     +\t\t\t\tstruct snd_usb_endpoint *ep);\n    -+int snd_usb_endpoint_prepare(struct snd_usb_audio *chip,\n    + int snd_usb_endpoint_configure(struct snd_usb_audio *chip,\n    +-\t\t\t       struct snd_usb_endpoint *ep);\n     +\t\t\t     struct snd_usb_endpoint *ep);\n      int snd_usb_endpoint_get_clock_rate(struct snd_usb_audio *chip, int clock);\n      \n      bool snd_usb_endpoint_compatible(struct snd_usb_audio *chip,\n     \n      ## sound/usb/pcm.c ##\n    -@@ sound/usb/pcm.c: static int configure_endpoints(struct snd_usb_audio *chip,\n    - \t\tif (stop_endpoints(subs, false))\n    - \t\t\tsync_pending_stops(subs);\n    - \t\tif (subs-\u003esync_endpoint) {\n    --\t\t\terr \u003d snd_usb_endpoint_configure(chip, subs-\u003esync_endpoint);\n    -+\t\t\terr \u003d snd_usb_endpoint_prepare(chip, subs-\u003esync_endpoint);\n    - \t\t\tif (err \u003c 0)\n    - \t\t\t\treturn err;\n    - \t\t}\n    --\t\terr \u003d snd_usb_endpoint_configure(chip, subs-\u003edata_endpoint);\n    -+\t\terr \u003d snd_usb_endpoint_prepare(chip, subs-\u003edata_endpoint);\n    - \t\tif (err \u003c 0)\n    - \t\t\treturn err;\n    - \t\tsnd_usb_set_format_quirk(subs, subs-\u003ecur_audiofmt);\n    - \t} else {\n    - \t\tif (subs-\u003esync_endpoint) {\n    --\t\t\terr \u003d snd_usb_endpoint_configure(chip, subs-\u003esync_endpoint);\n    -+\t\t\terr \u003d snd_usb_endpoint_prepare(chip, subs-\u003esync_endpoint);\n    - \t\t\tif (err \u003c 0)\n    - \t\t\t\treturn err;\n    - \t\t}\n     @@ sound/usb/pcm.c: static int snd_usb_hw_params(struct snd_pcm_substream *substream,\n      \tsubs-\u003ecur_audiofmt \u003d fmt;\n      \tmutex_unlock(\u0026chip-\u003emutex);\n```\n\nPlease refer to the contribution guidelines for details:\nhttps://android.googlesource.com/kernel/common/+/refs/heads/android-mainline/README.md\nAyeAye - CL 536706920 android-build.ayeaye-python-dispatcher_20230531.05_p0",
      "revId": "e111ea4a1f4a4b3333a4c9874fe3056f93f49c05",
      "serverId": "85c56323-6fa9-3386-8a01-6480fb634889"
    },
    {
      "robotId": "ayeaye",
      "robotRunId": "AR00000000000045932051",
      "url": "https://android-build.googleplex.com/builds/treetop/android-review/2527122?revision\u003d3",
      "fixSuggestions": [],
      "key": {
        "uuid": "ebf50ba3_8db73c03",
        "filename": "/COMMIT_MSG",
        "patchSetId": 3
      },
      "lineNbr": 0,
      "author": {
        "id": 1757831
      },
      "writtenOn": "2023-06-06T14:16:51Z",
      "side": 1,
      "message": "[Experimental] Cherry pick is not clean. These are the differences between the original change and this cherry-pick:\nChecked against UPSTREAM commit: [googlesource](https://android.googlesource.com/kernel/common/+/ff878b408a03bef5d610b7e2302702e16a53636e) [kernel.org](https://git.kernel.org/pub/scm/linux/kernel/git/torvalds/linux.git/commit/?id\u003dff878b408a03bef5d610b7e2302702e16a53636e)\n\n```\n      ## sound/usb/endpoint.c ##\n     @@ sound/usb/endpoint.c: bool snd_usb_endpoint_compatible(struct snd_usb_audio *chip,\n    @@ sound/usb/endpoint.c: bool snd_usb_endpoint_compatible(struct snd_usb_audio *chi\n       * Note that this function doesn\u0027t configure the endpoint.  The substream\n     - * needs to set it up later via snd_usb_endpoint_configure().\n     + * needs to set it up later via snd_usb_endpoint_set_params() and\n    -+ * snd_usb_endpoint_prepare().\n    ++ * snd_usb_endpoint_configure().\n       */\n      struct snd_usb_endpoint *\n      snd_usb_endpoint_open(struct snd_usb_audio *chip,\n    @@ sound/usb/endpoint.c: out_of_memory:\n      {\n      \tconst struct audioformat *fmt \u003d ep-\u003ecur_audiofmt;\n      \tint err;\n    -@@ sound/usb/endpoint.c: static int init_sample_rate(struct snd_usb_audio *chip,\n    +@@ sound/usb/endpoint.c: static int snd_usb_endpoint_set_params(struct snd_usb_audio *chip,\n      }\n      \n      /*\n     - * snd_usb_endpoint_configure: Configure the endpoint\n    -+ * snd_usb_endpoint_prepare: Prepare the endpoint\n    ++ * snd_usb_endpoint_configure: Prepare the endpoint\n       *\n       * This function sets up the EP to be fully usable state.\n     - * It\u0027s called either from hw_params or prepare callback.\n    @@ sound/usb/endpoint.c: static int init_sample_rate(struct snd_usb_audio *chip,\n       * The function checks need_setup flag, and performs nothing unless needed,\n       * so it\u0027s safe to call this multiple times.\n       *\n    -  * This returns zero if unchanged, 1 if the configuration has changed,\n    +@@ sound/usb/endpoint.c: static int snd_usb_endpoint_set_params(struct snd_usb_audio *chip,\n       * or a negative error code.\n       */\n    --int snd_usb_endpoint_configure(struct snd_usb_audio *chip,\n    + int snd_usb_endpoint_configure(struct snd_usb_audio *chip,\n     -\t\t\t       struct snd_usb_endpoint *ep)\n    -+int snd_usb_endpoint_prepare(struct snd_usb_audio *chip,\n     +\t\t\t     struct snd_usb_endpoint *ep)\n      {\n      \tbool iface_first;\n    @@ sound/usb/endpoint.h: snd_usb_endpoint_open(struct snd_usb_audio *chip,\n      \t\t      bool is_sync_ep);\n      void snd_usb_endpoint_close(struct snd_usb_audio *chip,\n      \t\t\t    struct snd_usb_endpoint *ep);\n    --int snd_usb_endpoint_configure(struct snd_usb_audio *chip,\n    --\t\t\t       struct snd_usb_endpoint *ep);\n     +int snd_usb_endpoint_set_params(struct snd_usb_audio *chip,\n     +\t\t\t\tstruct snd_usb_endpoint *ep);\n    -+int snd_usb_endpoint_prepare(struct snd_usb_audio *chip,\n    + int snd_usb_endpoint_configure(struct snd_usb_audio *chip,\n    +-\t\t\t       struct snd_usb_endpoint *ep);\n     +\t\t\t     struct snd_usb_endpoint *ep);\n      int snd_usb_endpoint_get_clock_rate(struct snd_usb_audio *chip, int clock);\n      \n      bool snd_usb_endpoint_compatible(struct snd_usb_audio *chip,\n     \n      ## sound/usb/pcm.c ##\n    -@@ sound/usb/pcm.c: static int configure_endpoints(struct snd_usb_audio *chip,\n    - \t\tif (stop_endpoints(subs, false))\n    - \t\t\tsync_pending_stops(subs);\n    - \t\tif (subs-\u003esync_endpoint) {\n    --\t\t\terr \u003d snd_usb_endpoint_configure(chip, subs-\u003esync_endpoint);\n    -+\t\t\terr \u003d snd_usb_endpoint_prepare(chip, subs-\u003esync_endpoint);\n    - \t\t\tif (err \u003c 0)\n    - \t\t\t\treturn err;\n    - \t\t}\n    --\t\terr \u003d snd_usb_endpoint_configure(chip, subs-\u003edata_endpoint);\n    -+\t\terr \u003d snd_usb_endpoint_prepare(chip, subs-\u003edata_endpoint);\n    - \t\tif (err \u003c 0)\n    - \t\t\treturn err;\n    - \t\tsnd_usb_set_format_quirk(subs, subs-\u003ecur_audiofmt);\n    - \t} else {\n    - \t\tif (subs-\u003esync_endpoint) {\n    --\t\t\terr \u003d snd_usb_endpoint_configure(chip, subs-\u003esync_endpoint);\n    -+\t\t\terr \u003d snd_usb_endpoint_prepare(chip, subs-\u003esync_endpoint);\n    - \t\t\tif (err \u003c 0)\n    - \t\t\t\treturn err;\n    - \t\t}\n     @@ sound/usb/pcm.c: static int snd_usb_hw_params(struct snd_pcm_substream *substream,\n      \tsubs-\u003ecur_audiofmt \u003d fmt;\n      \tmutex_unlock(\u0026chip-\u003emutex);\n```\n\nPlease refer to the contribution guidelines for details:\nhttps://android.googlesource.com/kernel/common/+/refs/heads/android-mainline/README.md\nAyeAye - CL 536706920 android-build.ayeaye-python-dispatcher_20230531.05_p0",
      "revId": "e111ea4a1f4a4b3333a4c9874fe3056f93f49c05",
      "serverId": "85c56323-6fa9-3386-8a01-6480fb634889"
    }
  ]
}